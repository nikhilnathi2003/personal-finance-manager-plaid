// Copy this file to config.js and fill in your values.
// config.js is git-ignored so your real key never lands in the repo.
export const CONFIG = {
  BACKEND_URL: 'https://YOUR-APP.onrender.com',
  API_KEY: '',
};
